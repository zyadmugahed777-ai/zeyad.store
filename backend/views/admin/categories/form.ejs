<%- include('../../partials/head', { title }) %>
<%- include('../../partials/sidebar', { active }) %>
<main class="admin-main">
  <%- include('../../partials/topbar') %>
  
  <div class="admin-content">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h1 class="page-title"><%= title %></h1>
      </div>
      <div class="admin-header-actions">
        <a href="/admin/categories" class="btn btn-outline">
          <span class="material-symbols-rounded">arrow_forward</span>
          العودة للقائمة
        </a>
      </div>
    </div>

    <form action="<%= category ? '/admin/categories/' + category.id + '/edit' : '/admin/categories/create' %>" method="POST" enctype="multipart/form-data">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      
      <div class="glass-card mb-4">
        <div class="row" style="display: flex; flex-wrap: wrap; margin: -12px;">
          <!-- Left Column: Primary Details -->
          <div style="flex: 0 0 66.66%; padding: 12px; box-sizing: border-box;">
            
            <div class="form-group">
              <label class="form-label">القسم الرئيسي <span class="text-danger">*</span></label>
              <select name="department_id" class="form-control" required>
                <option value="">اختر القسم الرئيسي...</option>
                <% departments.forEach(dep => { %>
                  <option value="<%= dep.id %>" <%= category && category.department_id === dep.id ? 'selected' : '' %>><%= dep.name_ar %></option>
                <% }) %>
              </select>
            </div>

            <div class="form-group mt-3">
              <label class="form-label">الاسم (عربي) <span class="text-danger">*</span></label>
              <input type="text" name="name_ar" class="form-control" value="<%= category ? category.name_ar : '' %>" required oninput="generateSlug(this.value)">
            </div>
            
            <div class="form-group mt-3">
              <label class="form-label">الاسم (إنجليزي)</label>
              <input type="text" name="name_en" class="form-control" value="<%= category ? category.name_en : '' %>" dir="ltr">
            </div>

            <div class="form-group mt-3">
              <label class="form-label">الرابط (Slug) <span class="text-danger">*</span></label>
              <input type="text" name="slug" id="slugInput" class="form-control" value="<%= category ? category.slug : '' %>" required dir="ltr">
            </div>

            <div class="form-group mt-4">
              <label class="form-label">الوصف</label>
              <textarea name="description_ar" class="form-control rich-editor" rows="4"><%= category ? category.description_ar : '' %></textarea>
              <small class="text-muted d-block mt-1">
                يُحفظ في قاعدة البيانات ويُرسل إلى الموقع ضمن بيانات التصنيفات، ويظهر في بطاقة التصنيف كما في المعاينة أدناه.
              </small>
            </div>

          </div>

          <!-- Right Column: Settings & Media -->
          <div style="flex: 0 0 33.33%; padding: 12px; box-sizing: border-box;">

            <div class="form-group">
              <label class="form-label">صورة التصنيف</label>
              <% if (category && category.image) { %>
                <div class="mb-2">
                  <img src="<%= category.image %>" alt="Current Image" style="width: 100%; height: 150px; object-fit: cover; border-radius: 8px; border: 1px solid var(--border-color);">
                </div>
              <% } %>
              <input type="file" name="image" class="form-control" accept="image/*">
            </div>

            <div class="form-group mt-4">
              <label class="form-label">الترتيب</label>
              <input type="number" name="sort_order" class="form-control" value="<%= category ? category.sort_order : '0' %>" dir="ltr">
            </div>

            <!-- How this category is drawn on the storefront. The four options
                 are the only shapes the frontend knows; the database refuses
                 anything else. All the categories of one department share the
                 shape of the first one that sets it, because mixing shapes in a
                 single row reads as a bug rather than as a design. -->
            <div class="form-group mt-4">
              <label class="form-label">شكل عرض الفئة في الموقع</label>
              <%
                const styles = [
                  ['',        'الافتراضي — بطاقة بصورة'],
                  ['card',    'بطاقة بصورة (الأنسب للصور الواقعية)'],
                  ['circle',  'دائرة (مناسب لعدد كبير من الفئات)'],
                  ['compact', 'مضغوط — صورة صغيرة والاسم بجانبها'],
                  ['pill',    'اسم فقط بدون صورة']
                ];
                const current = category && category.display_style ? category.display_style : '';
              %>
              <select name="display_style" class="form-control">
                <% styles.forEach(function (s) { %>
                  <option value="<%= s[0] %>" <%= current === s[0] ? 'selected' : '' %>><%= s[1] %></option>
                <% }) %>
              </select>
              <small class="text-muted" style="font-size:11px; display:block; margin-top:4px; line-height:1.7;">
                يحدد شكل الفئة في صف «تسوّق حسب الفئة». اتركه على الافتراضي إن لم تكن متأكداً —
                لن يتغير شيء في الموقع.
              </small>
            </div>

            <div class="form-group mt-4" style="background: var(--bg-tertiary); padding: 16px; border-radius: 8px;">
              <label style="display: flex; align-items: center; gap: 10px; cursor: pointer;">
                <input type="checkbox" name="is_active" <%= (!category || category.is_active) ? 'checked' : '' %> style="width: 20px; height: 20px;">
                <span style="font-weight: 600;">التصنيف نشط</span>
              </label>
            </div>

          </div>
        </div>
      </div>

      <%- include('../../partials/taxonomy-preview', { kind: 'category', record: category }) %>

      <div class="d-flex justify-content-end gap-3 mb-5">
        <a href="/admin/categories" class="btn btn-outline">إلغاء</a>
        <% if (category) { %>
          <a href="/category.html" target="_blank" class="btn btn-outline">
            <span class="material-symbols-rounded">visibility</span>
            معاينة التصنيف
          </a>
        <% } %>
        <button type="submit" class="btn btn-primary" style="padding: 12px 32px; font-size: 16px;">
          <span class="material-symbols-rounded">save</span>
          حفظ التغييرات
        </button>
      </div>

    </form>
  </div>
</main>

<script>
  function generateSlug(text) {
    if (document.getElementById('slugInput').value && '<%= category ? "true" : "" %>' === 'true') {
      return;
    }
    const slug = text.toLowerCase()
      .trim()
      .replace(/[^\w\s-]/g, '')
      .replace(/[\s_-]+/g, '-')
      .replace(/^-+|-+$/g, '');
    
    if(slug) {
      document.getElementById('slugInput').value = slug;
    }
  }
</script>
<%- include('../../partials/foot') %>
