<%- include('../../partials/head', { title }) %>
<%- include('../../partials/sidebar', { active }) %>
<main class="admin-main">
  <%- include('../../partials/topbar') %>
  
  <div class="admin-content">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h1 class="page-title"><%= title %></h1>
        <p class="page-description">إدارة الفئات الفرعية التابعة للأقسام الرئيسية.</p>
      </div>
      <div class="admin-header-actions">
        <a href="/admin/ai-employee" class="btn btn-ai-employee">
          <span class="material-symbols-rounded">neurology</span>
          الموظف الذكي
        </a>
        <a href="/admin/categories/create" class="btn btn-primary">
          <span class="material-symbols-rounded">add</span>
          إضافة تصنيف جديد
        </a>
      </div>
    </div>

    <!-- Filters -->
    <div class="glass-card mb-4">
      <form action="/admin/categories" method="GET" class="d-flex gap-3 align-items-center">
        <div style="flex: 1;">
          <input type="text" name="q" class="form-control" placeholder="البحث عن تصنيف..." value="<%= search %>">
        </div>
        <div style="width: 200px;">
          <select name="department_id" class="form-control">
            <option value="">جميع الأقسام</option>
            <% departments.forEach(dep => { %>
              <option value="<%= dep.id %>" <%= department_id == dep.id ? 'selected' : '' %>><%= dep.name_ar %></option>
            <% }) %>
          </select>
        </div>
        <div style="width: 150px;">
          <select name="status" class="form-control">
            <option value="">جميع الحالات</option>
            <option value="1" <%= status === '1' ? 'selected' : '' %>>نشط</option>
            <option value="0" <%= status === '0' ? 'selected' : '' %>>غير نشط</option>
          </select>
        </div>
        <button type="submit" class="btn btn-accent">
          <span class="material-symbols-rounded">search</span> بحث
        </button>
        <% if(search || status || department_id) { %>
          <a href="/admin/categories" class="btn btn-outline">إلغاء الفلترة</a>
        <% } %>
      </form>
    </div>

    <!-- Table -->
    <div class="glass-card">
      <div class="table-responsive">
        <table class="admin-table">
          <thead>
            <tr>
              <th width="60">صورة</th>
              <th>الاسم (عربي)</th>
              <th>الاسم (إنجليزي)</th>
              <th>القسم الرئيسي</th>
              <th>كود التصنيف</th>
              <th class="text-center">المنتجات</th>
              <th class="text-center">الحالة</th>
              <th class="text-center">الترتيب</th>
              <th class="text-center" width="150">إجراءات</th>
            </tr>
          </thead>
          <tbody>
            <% if (categories.length === 0) { %>
              <tr>
                <td colspan="9" class="text-center py-5">لا توجد تصنيفات مسجلة.</td>
              </tr>
            <% } %>
            <% categories.forEach(cat => { %>
              <tr>
                <td>
                  <% 
                    const catImg = cat.display_image || cat.image || '/assets/zeyad-category-showrooms.png';
                    const formattedCatImg = (catImg.startsWith('/') || catImg.startsWith('http') || catImg.startsWith('data:')) ? catImg : '/' + catImg;
                  %>
                  <a href="/admin/categories/<%= cat.id %>/edit" style="display: block; width: 44px; height: 44px; border-radius: 8px; overflow: hidden; border: 1px solid var(--border-color, rgba(255,255,255,0.1)); background: #000;">
                    <img src="<%= formattedCatImg %>" alt="<%= cat.name_ar %>" onerror="this.onerror=null;this.src='/assets/zeyad-category-showrooms.png';" style="width: 100%; height: 100%; object-fit: cover;">
                  </a>
                </td>
                <td><strong><%= cat.name_ar %></strong></td>
                <td><span class="text-muted" dir="ltr"><%= cat.name_en || '-' %></span></td>
                <td>
                  <a href="/admin/departments?q=<%= cat.department_name %>" class="text-decoration-none">
                    <%= cat.department_name %>
                  </a>
                </td>
                <td><code dir="ltr"><%= cat.code %></code></td>
                <td class="text-center">
                  <a href="/admin/products?category_id=<%= cat.id %>" class="badge badge-warning"><%= cat.products_count %> منتج</a>
                </td>
                <td class="text-center">
                  <% if (cat.is_active) { %>
                    <span class="badge badge-success">نشط</span>
                  <% } else { %>
                    <span class="badge badge-danger">غير نشط</span>
                  <% } %>
                </td>
                <td class="text-center"><%= cat.sort_order %></td>
                <td class="text-center">
                  <div class="d-flex justify-content-center gap-2">
                    <a href="/admin/categories/<%= cat.id %>/edit" class="btn-icon" title="تعديل">
                      <span class="material-symbols-rounded">edit</span>
                    </a>
                    <form action="/admin/categories/<%= cat.id %>/delete" method="POST" class="delete-form" style="display:inline;">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                      <button type="submit" class="btn-icon text-danger" title="حذف" <%= (cat.products_count > 0) ? 'disabled' : '' %>>
                        <span class="material-symbols-rounded">delete</span>
                      </button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

      <!-- Pagination -->
      <% if (totalPages > 1) { %>
        <div class="d-flex justify-content-between align-items-center mt-4">
          <div class="text-muted" style="font-size: 13px;">
            إجمالي التصنيفات: <%= totalItems %>
          </div>
          <div class="d-flex gap-2">
            <% if (page > 1) { %>
              <a href="?page=<%= page - 1 %>&q=<%= search %>&status=<%= status %>&department_id=<%= department_id %>" class="btn btn-outline">السابق</a>
            <% } %>
            <% for(let i = 1; i <= totalPages; i++) { %>
              <a href="?page=<%= i %>&q=<%= search %>&status=<%= status %>&department_id=<%= department_id %>" class="btn <%= page === i ? 'btn-primary' : 'btn-outline' %>"><%= i %></a>
            <% } %>
            <% if (page < totalPages) { %>
              <a href="?page=<%= page + 1 %>&q=<%= search %>&status=<%= status %>&department_id=<%= department_id %>" class="btn btn-outline">التالي</a>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</main>
<%- include('../../partials/foot') %>
